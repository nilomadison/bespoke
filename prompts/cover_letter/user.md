<!--
Maintainer note: kept as a plain hand-off. The previous user prompt re-stated some
guidance that lived in the system prompt; that has been removed to avoid repetition
that the model treats as emphasis.
-->
Write a cover letter for the following application.

Target role: {job_title} at {company_name}

Job analysis (signals from the JD — useful for deciding what to emphasize, not for parroting back at the reader):
{analysis_json}

Career data — every concrete claim in the letter must trace to something in here:
{plan_context}
